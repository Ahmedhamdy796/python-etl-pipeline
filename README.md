# python-etl-pipeline
The goal is to build a functional, automated ETL (Extract, Transform, Load) pipeline designed to handle heterogeneous data sources (CSV and JSON) and prepare them for downstream analysis.
